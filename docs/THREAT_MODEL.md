// @review todo

compromised defillama/cloudflare worker api could potentially enforce sub-optimal strategy allocation. accepted risk - CRE workflow can be paused to prevent it, funds would only ever be rebalanced to a pre approved strategy anyway, and then rebalanced back to the optimal position after
