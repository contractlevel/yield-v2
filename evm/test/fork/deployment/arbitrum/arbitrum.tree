Arbitrum Deployment Fork

GIVEN the six-chain fork topology is deployed
  WHEN the active fork is Arbitrum
    THEN the child deployment is configured with Arbitrum mainnet addresses
    AND the child vault is deployed as a UUPS proxy with the upgrader role granted
    AND the child vault trusts the Base parent vault
