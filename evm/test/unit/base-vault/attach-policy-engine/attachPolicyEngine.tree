attachPolicyEngine
  ├── when the caller does not have the COMPLIANCE_OPERATOR_ROLE
  │   └── it should revert
  └── when the caller has the COMPLIANCE_OPERATOR_ROLE
      ├── it should update the policy engine
      └── it should emit a PolicyEngineAttached event
