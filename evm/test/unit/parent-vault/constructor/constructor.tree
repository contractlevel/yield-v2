ParentVault_constructor
  ├── it should set i_thisChainSelector
  ├── it should set i_link
  ├── it should set i_asset
  ├── it should set i_assetPrecision
  ├── it should set i_sharePrecision
  ├── it should set i_minDepositAmount
  ├── it should set the emergency receiver
  ├── it should set i_yieldcoinToken
  ├── it should set s_treasury
  ├── it should set the CCIP router
  ├── it should attach the policy engine
  ├── it should grant DEFAULT_ADMIN_ROLE to defaultAdmin
  ├── it should grant PAUSER_ROLE to pauser
  ├── it should grant UNPAUSER_ROLE to unpauser
  ├── it should grant CONFIG_OPERATOR_ROLE to configOperator
  ├── it should grant POLICY_ENGINE_MANAGER_ROLE to policyEngineManager
  ├── it should set s_defaultCcipGasLimit
  ├── it should set the rebalance nonce to 1
  ├── it should set the performance fee high water mark to i_sharePrecision
  ├── it should set the epoch nonce to 1
  ├── it should set the first epoch to OPEN
  ├── it should set the first epoch open timestamp to block.timestamp
  ├── it should set lastRebalanceCompletedTimestamp to block.timestamp
  ├── it should leave initial active protocol adapter set false
  ├── it should leave the active strategy protocol ID unset
  ├── it should leave the active strategy chain selector unset
  ├── it should leave the active strategy protocol adapter unset
  ├── when treasury is the zero address
  │   └── it should revert with BaseVault__NoZeroAddress
  ├── when share is the zero address
  │   └── it should revert with BaseVault__NoZeroAddress
  └── when policy engine manager is the zero address
      └── it should revert with BaseVault__NoZeroAddress
